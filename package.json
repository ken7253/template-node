{
  "name": "template-node",
  "version": "0.0.0",
  "description": "(Node.js && TypeScript && ESM) Template",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
